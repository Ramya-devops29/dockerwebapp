version: "3.9"

services:
  devopsdb:
    image: shaikmustafa/automation:db
    ports:
      - "3306:3306"
    networks:
      - mynetwork

  application:
    image: shaikmustafa/automation:app
    ports:
      - "2222:8080"
    networks:
      - mynetwork
networks:
  mynetwork:
    driver: bridge
